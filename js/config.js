/**
 * Optional client-side notification (Discord webhook URL is visible in page source).
 * Prefer Vercel env var DISCORD_WEBHOOK_URL + /api/notify instead.
 */
window.PROPOSAL_CONFIG = {
  DISCORD_WEBHOOK_URL: '',
};
